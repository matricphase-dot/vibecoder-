let counter = 0; const counterElement = document.getElementById('counter'); const minusButton = document.getElementById('minus-button'); const plusButton = document.getElementById('plus-button'); function updateCounter() { counterElement.textContent = counter; } function handleMinusButtonClick() { counter = Math.max(counter - 1, 0); updateCounter(); } function handlePlusButtonClick() { counter++; updateCounter(); } minusButton.addEventListener('click', handleMinusButtonClick); plusButton.addEventListener('click', handlePlusButtonClick); updateCounter(); window.addEventListener('load', () => { console.log('Page loaded'); }); window.addEventListener('error', (event) => { console.error('Error occurred:', event); });