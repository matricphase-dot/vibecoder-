let counter = 0; const counterDisplay = document.getElementById('counter-display'); const minusButton = document.getElementById('minus-button'); const plusButton = document.getElementById('plus-button'); function updateCounterDisplay() { counterDisplay.textContent = counter; } minusButton.addEventListener('click', () => { counter--; updateCounterDisplay(); }); plusButton.addEventListener('click', () => { counter++; updateCounterDisplay(); });